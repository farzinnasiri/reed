## What to build

Decide on canonical naming between `workout-*` component files and `liveSessions` schema. Currently the schema uses `liveSessions` but all workout-related components use `workout-*` naming. This creates confusion about whether "workout" and "live session" are the same thing or different concepts.

## Acceptance criteria

- [ ] Decide: do file names match schema (`liveSession-*`) or does schema match files (`workoutSessions`)?
- [ ] Document the naming rule in `CONTEXT.md`
- [ ] Create follow-up issue for the actual rename

## Blocked by

None - can start immediately

Status: needs-triage
