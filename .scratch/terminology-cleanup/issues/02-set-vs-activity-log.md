## What to build

Users log "sets" but the database table is `activityLogs`. Decide if the table should be renamed or if the split (set = user action, activity log = system record) is acceptable long-term.

## Acceptance criteria

- [ ] Decide canonical naming for user term, code term, and table name
- [ ] Update `CONTEXT.md` with the decision
- [ ] Create follow-up issue for any renames

## Blocked by

None - can start immediately

Status: needs-triage
