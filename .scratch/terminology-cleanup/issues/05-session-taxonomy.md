## What to build

"Session" currently means "live session" in the schema (`liveSessions`) but "any workout instance" in conversation. This will create ambiguity when planned sessions are added. Resolve the taxonomy now.

## Acceptance criteria

- [ ] Decide naming for: umbrella term, live session, planned session
- [ ] Update `CONTEXT.md` with session taxonomy
- [ ] Create follow-up issue to rename `liveSessions` if needed

## Blocked by

None - can start immediately

Status: needs-triage
