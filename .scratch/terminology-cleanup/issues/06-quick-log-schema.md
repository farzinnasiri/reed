## What to build

Quick logs feel like standalone activities to users ("I walked for 20 minutes") but are forced into the exercise + set schema (`activityLogs` with `source: 'quick_log'`). Evaluate if this alignment is correct or needs adjustment.

## Acceptance criteria

- [ ] Decide: is every quick log "one set of one exercise" the right model?
- [ ] If not, sketch alternative schema for standalone activities
- [ ] Document decision in `CONTEXT.md`

## Blocked by

None - can start immediately

Status: needs-triage
