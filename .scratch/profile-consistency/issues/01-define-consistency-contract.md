## What to build

Define the canonical product contract for the Profile `Consistency` module so Reed rewards training rhythm without encouraging unhealthy daily streak behavior. This issue should lock the user-facing metric semantics for v1, including how weekly cadence targets map to "on target" weeks, what the primary summary line says, and whether any `streak` language is allowed.

## Acceptance criteria

- [ ] Define the v1 consistency contract for each current onboarding weekly cadence option.
- [ ] Decide the canonical user-facing terminology and copy rules for the module, including whether `streak` is explicitly disallowed.
- [ ] Define the v1 summary outputs Reed should support: current week progress, recent on-target rate, and current run of on-target weeks.
- [ ] Record the decision in the relevant product/domain docs that future work should follow.

## Blocked by

None - can start immediately

Status: needs-triage
